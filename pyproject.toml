[build-system]
requires = ["setuptools>=69", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "vps-ssh-launcher"
version = "1.1.1"
requires-python = ">=3.11"
dependencies = ["paramiko>=5.0.0,<6.0.0"]

[project.scripts]
vps-maint = "vps_ssh_launcher.maintenance_cli:main"

[project.optional-dependencies]
installer = ["pexpect>=4.9.0"]
dev = [
    "bandit>=1.8.0",
    "mypy>=1.13.0",
    "pexpect>=4.9.0",
    "pip-audit>=2.7.0",
    "pytest>=8.0.0",
    "ruff>=0.8.0",
    "types-pexpect>=4.9.0",
]

[tool.setuptools]
py-modules = ["ssh_tool", "auto_install"]
packages = ["vps_ssh_launcher", "vps_ssh_launcher.maintenance"]

[tool.mypy]
python_version = "3.11"
warn_return_any = true
warn_unused_configs = true
disallow_untyped_defs = true

[[tool.mypy.overrides]]
module = ["paramiko.*"]
ignore_missing_imports = true

# PyYAML ships no inline stubs and types-PyYAML is not a dev dependency; the
# remote CPA scripts and their tests only use safe_load/safe_dump.
[[tool.mypy.overrides]]
module = ["yaml.*"]
ignore_missing_imports = true

[tool.ruff.lint]
select = ["E4", "E7", "E9", "F"]

[tool.pytest.ini_options]
# Enumerate the suite explicitly: the repo root also holds the live
# sciman-v2ray-agent upstream checkout whose test tree must not be collected.
testpaths = [
    "test_ssh_tool.py",
    "test_auto_install.py",
    "test_cpa_admission.py",
    "test_scripts.py",
    "test_integration_real_ssh.py",
    "test_maintenance.py",
]
python_files = ["test_*.py"]
